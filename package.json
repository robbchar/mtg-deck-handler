{
  "name": "mtg-deck-manager",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently \"firebase emulators:start --only auth,firestore,functions,hosting\" \"npm run dev --workspace=client\"",
    "dev:server": "npm run dev --workspace=server",
    "start": "node server/index.js",
    "test": "npm run test:server && npm run test:client",
    "test:server": "npm run test --workspace=server",
    "test:client": "npm run test --workspace=client",
    "deploy:hosting": "npm run build --workspace=client && firebase deploy --only hosting --project robbchar-3db11",
    "deploy:functions": "firebase deploy --only functions:mtgApi --project robbchar-3db11",
    "deploy": "npm run build --workspace=client && firebase deploy --only hosting,functions:mtgApi --project robbchar-3db11"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
